<div class="col-sm-4">
  <div class="row">
    <div class="col-sm-8">
      <h3>Employments</h3>
    </div>
    <div class="col-sm-4">
      <br>
      <%= link_to new_employment_path(person: @person), class: 'btn btn-sm btn-default pull-right' do %>
        <span class="glyphicon glyphicon-plus"></span> New
      <% end %>
    </div>
  </div>
  
  <% if @person.employments.size > 0 %>
    <table class="table">
      <% @person.employments.each do |e| %>
        <tr>
          <td width="60">
            <%= link_to e do %>
              <% if e.place.num_photos > 0 %>
                <%= image_tag e.place.default_photo.image.event_thumb.url %>
              <% else %>
                <img data-src="holder.js/50x50" />
              <% end %>
            <% end %>
          </td>
          <td>
            <%= link_to e.place.name, e %><br>
            <%= e.title %>
          </td>
        </tr>
      <% end %>
    </table>
  <% else %>
    <p>No employments.</p>
  <% end %>
</div>