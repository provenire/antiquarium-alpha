<ul class="nav navbar-nav navbar-right">
    
    <% if user_signed_in? %>
    
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-plus"></span> <b class="caret"></b></a>
          <ul class="dropdown-menu">
              <li role="presentation" class="dropdown-header">Create New</li>
              <li>
                  <%= link_to new_artifact_path do %>
                    <span class="glyphicon glyphicon-glass"></span> Artifact
                  <% end %>
              </li>
              <li>
                <%= link_to new_person_path do %>
                    <span class="glyphicon glyphicon-user"></span> Person
                <% end %>
              </li>
              <li>
                <%= link_to new_place_path do %>
                    <span class="glyphicon glyphicon-map-marker"></span> Place
                <% end %>
              </li>
          </ul>
        </li>
        
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %> <b class="caret"></b></a>
          <ul class="dropdown-menu">
              <li>
                <%= link_to profile_path(current_user) do %>
                    <span class="glyphicon glyphicon-user"></span> Profile
                <% end %>
              </li>
              <li>
                  <%= link_to edit_registration_path(current_user) do %>
                      <span class="glyphicon glyphicon-cog"></span> Settings
                  <% end %>
              </li>
              <li class="divider"></li>
              <li>
                  <%= link_to destroy_user_session_path do %>
                      <span class="glyphicon glyphicon-log-out"></span> Logout
                  <% end %>
              </li>
          </ul>
      </li>
    
    <% else %>
    
        <li><%= link_to 'Register', new_registration_path(User) %></li>
		<li><%= link_to 'Login', new_user_session_path %></li>
    
    <% end %>
    
</ul>