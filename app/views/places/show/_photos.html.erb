<div class="col-lg-4">
  <div class="row">
    <% if @place.num_photos == 0 %>
      <div class="col-lg-12">
        <img data-src="holder.js/360x240/text: No photos available" class="img-responsive">
      </div>
    <% elsif @place.num_photos == 1 %>
      <div class="col-lg-12">
        <%= link_to @place.default_photo do %>
          <%= image_tag @place.default_photo.image.object_single.url, class: 'img-responsive' %>
        <% end %>
      </div>
    <% else %>
      <div class="col-lg-9">
        <%= link_to @place.default_photo do %>
          <%= image_tag @place.default_photo.image.object_main.url, class: 'img-responsive' %>
        <% end %>
      </div>
      <div class="col-lg-3">
        <% @place.next_photos.each do |p| %>
          <div class="row">
            <div class="col-lg-12">
              <%= link_to p do %>
                <%= image_tag p.image.object_thumb.url, class: 'img-responsive' %>
              <% end %>
            </div>
          </div>
          <br/>
        <% end %>
      </div>
    <% end %>
  </div>
  <div class="row">
    <div class="col-lg-10 col-lg-offset-1">
      <% if @place.num_photos > 0 %>
        <%= link_to "See all #{@place.num_photos} photos", place_photos_path(@place)%> | 
      <% end %>
      <a href="#" data-toggle="modal" data-target="#upload_photo_modal">+ Upload new photo</a>
    </div>
  </div>
</div>